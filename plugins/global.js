// import Vue from 'vue'

// https://github.com/nuxt/components

// NOTE: [20201130] - Components in /components are now automatically imported and registered when used within a
// a page template. It's still necessary to import/register to make available to content markdown pages though. Alternatively,
// could place in /components/global will "globally" registers components by default, but that seems inefficient.

// import BlogPostPhotos from '~/components/BlogPostPhotos.vue'
// import BlogPostPhotosSimple from '~/components/BlogPostPhotosSimple'

// Vue.component('BlogPostPhotos', BlogPostPhotos)
// Vue.component('BlogPostPhotosSimple', BlogPostPhotosSimple)
